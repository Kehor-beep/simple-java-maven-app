FROM maven:3.9-eclipse-temurin-21

# Install git, docker CLI, and ssh client
RUN apt-get update && \
    apt-get install -y git docker.io openssh-client && \
    rm -rf /var/lib/apt/lists/*

# Jenkins will use this as its workspace
WORKDIR /workspace

